import 'package:flutter/widgets.dart';

class HomePageHeaderLoader extends StatelessWidget {
  const HomePageHeaderLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
